# 扩岛与自主生活验收 · 2026-09-18

## 本轮结果

在保留原动态版文件与角色资产的基础上，生成 `assets/expanded/island_expanded.blend`、`island_expanded.glb`、`scene.json`，并将当前 viewer 接到扩建版。

- 地表平面 1.28 × 1.38，投影面积增加 76.64%；住宅、咖啡馆与人物保持原尺寸。
- 0.3 米网格采样的可走区域为原来的 2.091 倍。增加了四棵果树、庭院雨棚、咖啡馆遮雨棚、工作台、桶、果筐、花箱、长椅和连接步道。
- 七个自主生活地点；分早晨、白天、傍晚、晚间、夜间安排日程。雨天优先寻找最近可达雨棚。
- 玩家接管取消自主路线和当前动作，退出后从当前位置继续生活。提供观景跟随按钮。
- 地形、桥面、导航网格、碰撞、雨水范围、河道粒子和镜头同步调整。
- 沿用原存档，增加布局版本标记，旧位置只转换一次。作物、金币、菜篮和订单保持原值。
- 窄屏日记可展开与收起，点击目的地后收起，以减少场景遮挡。

## 自动检查

- `node scripts/test_resident.mjs`：7 项通过。包括面积与建筑尺寸、全部生活/菜田/咖啡馆地点两两路径及实际步进、日间完整活动、早晨工作台与夜间停留、雨天打断和恢复、暂停/手动接管、存档迁移幂等性。
- `node scripts/test_living.mjs`：4 项通过。原种植订单、存档、寻路和碰撞检查。
- `node scripts/test_world.mjs`：5 项通过。昼夜、保存、天气渐变检查。
- `scripts/validate_expansion.cjs`：490,128 三角面，20,859,068 字节，19 个独立控制节点；glTF Validator 0 errors、0 warnings、0 infos、0 hints。预算低于 55 万三角面和 25 MB。
- 相关 JS/MJS 语法检查与 `git diff --check` 通过。

## 浏览器观察

在本地 Codex 内置浏览器验证：

- 默认观景中，状态依次出现果园活动、菜苗浇水、咖啡馆停留和花草照料；人物随路线移动。
- 「看看她」进入人物跟随镜头；观察到雨天抵达小院雨棚后显示「檐下听雨」，角色位于实体屋顶下。
- 晴天 22:00，从咖啡馆侧步行回小院，显示「回小院休息」，到达后显示「在廊下休息」；夜间窗灯、星空和萤火虫仍显示。
- 「进入小岛」可以接管，去果树林按钮能通过石桥到新果园；退出恢复自主日程。日记仍显示菜篮 2、金币 0。
- 最终窄屏日记默认收起，展开后可用「去果树林」「去溪边」，选择目的地后收起。
- 检查了约 390 像素窄屏和桌面工具栏布局；临时桌面视口覆盖已 reset。
- 页面显示约 60 FPS；这只是本机观测，不代表真实手机性能基准。

## 当前边界

本轮完成扩岛布局、连通路线和观景自主生活基础。日程行为只表现日常生活，不收割作物、不增减库存、不花钱、不交订单。沿用五段现有骨骼动作，尚没有独立坐姿、饮茶、室内睡眠或精确手部接触。夜间在廊下休息；房屋室内尚未建造。关闭页面后不会模拟离线生活。植被、道路疏密和建筑细节仍可继续精修，远景岛仍不可前往。

新增资产通过本地 Blender 完成，本轮没有调用 Tripo 或消耗其积分。
