# 地形、河岸与树木验收

2026-09-18，本地 Blender 5.2.1 与现有 Three.js 项目。读取上轮参考布局源文件，输出独立的 `assets/landscape`，没有覆盖旧场景或使用 Tripo 额度。

## 交付与画面

- 树木从 11 棵增加到 26 棵，包含从旧源文件恢复的粉色花树。新增树木主要位于后坡、两侧与岛缘；粉色树在住宅右侧，中央休闲区和步道保持开放。
- 增加后坡、住宅缓坡、低丘、起伏地表和不规则岛缘；岩块、苔藓、花丛和垂藤连接草地与岩壁。
- 保留原河湾、桥和分区，溪水从上游约 13.755 米下降到下游 10.055 米。增加岸坡、浅水石、芦草、细水纹和瀑布水帘；末端使用顶点透明度渐隐。
- 主视角和俯视 Blender 渲染均已检查。渲染中的成熟作物与原黄昏天空用于预览，网页仍使用当前种植状态与动态天空、远景群岛。

## 自动验证

- `node scripts/test_layout.mjs landscape`：6 项通过，包括新场景全部生活地点与菜田之间的连续移动、过桥、白天/清晨日程、雨棚与夜间回家、玩家接管和存档迁移。
- `node scripts/test_landscape.mjs`：3 项通过，确认 Blender 与运行时高度一致、粉色树及树木数量、中央开放度、水流高程单调下降和岛缘安全。
- 原 `test_living` 4 项、`test_world` 5 项、`test_resident` 7 项、`test_layout` 6 项回归通过。
- `validate_landscape.cjs`：glTF Validator 0 错误、0 警告，569,491 三角面、23,895,408 字节、20 个控制节点。额外检查了实际 GLB 中瀑布顶点透明度的高低范围和 BLEND 材质。
- 修复了 Blender 导出器对“仅连接顶点 Alpha”节点不完整支持的问题，同时连接顶点颜色与透明度后重新导出，实际渐隐数据校验通过。
- 修改的运行时模块语法检查及 `git diff --check` 通过。

## 浏览器验证

本机默认 1280×720 页面检查了黄昏与正午下的树丛、粉色花树、坡地和水面。进入小岛后点击「去咖啡馆」，角色成功到达右岸雨棚，显示「云边咖啡／还需要 1 份蔬菜」。旧存档仍为菜篮 2、金币 0，未执行播种、收获或交付。自主生活状态随时间变化，保留日程与手动接管。

观察期间界面显示约 60–120 FPS，仅代表当时本机状态，并非性能保证。地图采用高度函数与简化碰撞；坡地、岩壁和水体仍是风格化实时场景，不是完整物理或流体模拟。没有新增坐姿、喝咖啡、室内或离线自主生活。
