# Tripo 精细人物验收 · 2026-09-17

## 交付结果

- 默认人物替换为 Tripo 精细版，人物工作室保留 `?model=local` 初版对照。
- 原始高模 1,894,963 三角面，4K base color / ORM / normal 纹理；独立 Blender 源文件保留。
- 网页 GLB：13,036,428 字节，154,379 三角面（含壶），37 骨，5 段骨骼动画。
- 实际费用：生成 60 + 绑定检查 0 + 绑定 25 = 85 积分；核对余额 515、冻结 0。无付费重抽、减面或动作重定向。

## 本地处理与观察

Blender 5.2.1 完成坐标与尺寸归一、减面、自动骨架名称映射、髋膝校正、发束/挎包/裙摆辅助骨与局部蒙皮修正。按下缘连通表面识别双腿，避免把露出的膝盖划入裙摆权重。最多四骨影响，所有权重归一；Blender 使用与网页一致的线性蒙皮。

检查了实际渲染的全身、面部、背面、行走、播种、浇水和收获姿态，见 `renders/character_tripo/`。修正前的膝盖横向折痕和裙摆内层拉扯已经处理。眼睑、嘴部、手部仍有生成网格痕迹；没有面部表情、独立手指、布料模拟或足部 IK。图像和网页是当前真实结果，不表示逐像素匹配原画。

## 自动验证

`GLTF_VALIDATOR_MODULE=/private/tmp/island-gltf-validator/package node scripts/validate_character.cjs --tripo`

- glTF Validator：0 errors、0 warnings。
- 骨架、蒙皮属性、五个命名动作、动画旋转采样、浇水壶、纹理、三角面和文件预算检查通过。
- Blender 导出器对 ORM 共用贴图的多个图像节点提示采用第一个 sampler；最终 glTF 校验无警告，网页材质检查正常。

`node scripts/test_living.mjs`：4 项通过，覆盖种植订单完整循环、存档恢复、菜田和咖啡馆可达性、河流/房屋/桥碰撞路径。三个角色相关 JS 文件语法检查及 `git diff --check` 通过。

## 浏览器实测

使用本机 Codex 内置浏览器和 `http://127.0.0.1:8765/`：

- 精细版、初版对照均能加载；面部近景、全身、行走和浇水切换正常，浇水壶显示。
- 桌面双栏与当前窄屏纵向布局可见，版本切换和动作控件可访问。
- 进入小岛后精细人物出现在已有存档的位置，完成生菜播种 → 浇水 → 18 秒生长 → 收获，互动期间按钮禁用，结束后恢复。
- 收获使菜篮从 1 增至 2，保留原有进度，没有重置存档。
- 点击南瓜田目的地后人物从生菜田自动步行到达，附近互动目标同步切换为南瓜田。
- 人物页和小岛页捕获的浏览器 error/warn 日志为空。小岛 UI 当时显示 60 FPS，仅代表本机当前视角，不作为手机性能保证。

制作与使用说明见 `assets/character_tripo/README.md`；生成参数和费用见 `tripo_character_generation.json`。本地预览需要 HTTP 服务器运行，可双击项目的 `start_viewer.command`。
