# 参考布局重组验收

本机日期：2026-09-18。仅使用本地 Blender 和已有资产，未调用 Tripo。旧版文件与用户已有改动保留，未暂存或提交。

## 画面与结构

从动态版可编辑源文件构建 `assets/layout/island_layout.blend`，重新生成地形、曲线河道、瀑布、路径和景观分组。住宅左上、菜田和羊圈左侧下方、咖啡馆右上、桥右下；保留建筑尺寸。中央区域以连贯草地、石径、长椅与小广场组织，河道在桥上游有横向转弯，宽度由 1.6 米增至 6.8 米。

检查了主视角 1200×900 和俯视 1100×1000 的 Blender 渲染，修复瀑布出口末端截面的折叠，并校正水面法线。离线渲染使用原黄昏天空，网页使用动态天空、远景岛和存档中的种植状态。

## 自动验证

- `node scripts/test_layout.mjs`：6 项通过，涵盖分区、建筑尺寸、中央可走区域、河流走向、全部生活地点与菜田之间的连续移动、过桥、日程、避雨、夜间回家、玩家接管及存档迁移。
- `node scripts/test_living.mjs`：4 项通过，种植订单、存档与原场景导航回归。
- `node scripts/test_world.mjs`：5 项通过，昼夜天气、暂停和存储回归。
- `node scripts/test_resident.mjs`：7 项通过，前一版扩建导航、日程与迁移回归。
- `scripts/validate_layout.cjs`：glTF Validator 0 错误、0 警告；500,151 三角面、20,600,572 字节、19 个独立控制节点。文件在 550,000 三角面和 25 MB 预算内。
- 修改的运行时 JavaScript 语法检查通过；`git diff --check` 通过。

## 浏览器

在本机 127.0.0.1:8765 的默认 1280×720 预览中检查新场景。夜间窗灯与路灯亮起，自主角色显示「在廊下休息」。切到正午后进入漫游，点击「去咖啡馆」，人物到达新右岸雨棚，显示「云边咖啡／还需要 1 份蔬菜」。存档仍为菜篮 2、金币 0，没有收获或交付操作。浏览器 console 检查无错误或警告。该次界面显示 120 FPS，不代表其他设备表现。

## 剩余差异

本轮完成构图和空间组织校准，并非逐像素复刻。原参考的上游山地、岩壁不规则形状、密集植被、自然岸线和微小生活物件仍可继续精修。导航仍为高度函数与简化碰撞；坐姿、室内睡眠和离线自主生活没有在本轮添加。
